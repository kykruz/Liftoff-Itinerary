@using Reviews.Models;

@model List<IEnumerable<Review>>

<form asp-controller="Review" asp-action="Create">
<input type="text">
<label for="content"></label>
<input type="submit" asp-action="Index">

</form>

@* @model Reviews.Models.Review

<form asp-action="Create" asp-controller="Review" method="post">
    <div class="form-group">
        <label for="title">Title:</label>
        <input type="text" class="form-control" id="title" name="Title" required>
    </div>
    <div class="form-group">
        <label for="author">Author:</label>
        <input type="text" class="form-control" id="author" name="Author" required>
    </div>
    <div class="form-group">
        <label for="content">Content:</label>
        <textarea class="form-control" id="content" name="Content" rows="3" required></textarea>
    </div>
    <button type="submit" class="btn btn-primary">Submit Review</button>
</form> *@
















 @* <h2>Review</h2>
@if (Model.Count > 0)
{
    @foreach (var review in Model)
    {
        <div class="card mb-3">
    <div class="card-body">
        <h5 class="card-title">@reviews.Author</h5>
        <p class="card-text">@review.Title</p>
        <p class="card-text"><small class="text-muted">@review.Content</small></p>
    </div>
</div>
    }
}

@if (User.Identity.IsAuthenticated)
{
<h3>Add a Comment</h3>
<form asp-action="AddComment" asp-controller="Member" method="post">
<div class="form-group">
<textarea class="form-control" rows="3" name="content" required></textarea>
</div>
<button type="submit" class="btn btn-primary">Submit</button>
</form>
}
else
{
<p>You need to <a asp-action="Login" asp-controller="Account">login</a> to leave a comment.</p>
}   *@
